# Core dependencies for content_creation pipeline
# Install with: pip install -r requirements.txt
#
# Shared layer (config, publish, notify) — see requirements-shared.txt
# This file covers runtime generators and HTTP dependencies.

# Config schema and validation
pydantic>=2.0,<3.0
pyyaml>=6.0

# Env management
python-dotenv>=1.0

# HTTP client (Suno REST API, image downloads)
requests>=2.31

# Retry / exponential backoff (Suno polling)
tenacity>=8.2.3

# Audio processing
pydub>=0.25.1

# YouTube publishing (Phase 5)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# Notifications (Phase 4)
discord-webhook>=1.3.0
slack-sdk>=3.30.0
