---
phase: 01-config-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - config/profiles/lofi_study.yaml
  - config/profiles/tech_tutorial.yaml
  - config/profiles/cinematic.yaml
  - .gitignore
autonomous: true
requirements:
  - CONF-01
  - CONF-03

must_haves:
  truths:
    - "User can load any of the three named profiles by passing its file path to PipelineConfig.from_yaml()"
    - "Each profile YAML contains profile-specific prompts, moods, and post-production settings that differ meaningfully between profiles"
    - "credentials/, configs/, and .env are gitignored — no secrets land in the repo"
    - "PipelineConfig.from_yaml() successfully validates all three profile files without error"
  artifacts:
    - path: "config/profiles/lofi_study.yaml"
      provides: "lofi-study named profile — warm ambient study mood"
    - path: "config/profiles/tech_tutorial.yaml"
      provides: "tech-tutorial named profile — clean professional tutorial mood"
    - path: "config/profiles/cinematic.yaml"
      provides: "cinematic named profile — high production value mood"
    - path: ".gitignore"
      provides: "Gitignore rules for credentials/, configs/, .env, youtube_token.pickle"
  key_links:
    - from: "config/profiles/*.yaml"
      to: "config/pipeline_config.PipelineConfig"
      via: "PipelineConfig.from_yaml(path)"
      pattern: "from_yaml"
---

<objective>
Create three starter named profile YAML files under config/profiles/ and lock down the gitignore for secrets.

Purpose: Named profiles are the user-facing feature of CONF-03 — a user selects "lofi_study" and gets the right prompts, effects, and post settings automatically. The gitignore protects credentials from accidental commits.
Output: Three profile YAML files loadable by PipelineConfig.from_yaml(), and an updated .gitignore.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-config-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write three named profile YAML files under config/profiles/</name>
  <files>config/profiles/lofi_study.yaml, config/profiles/tech_tutorial.yaml, config/profiles/cinematic.yaml</files>
  <action>
Create `config/profiles/` directory. Write three YAML files. Each file must be valid against the PipelineConfig schema defined in 01-01 — every key must match a field name in the Pydantic model exactly. Fields not set will use model defaults, so only override what is profile-specific.

**config/profiles/lofi_study.yaml**
```yaml
profile: lofi-study

video:
  style_prompt: "cozy study room, warm Edison bulb lighting, rainy window, soft bokeh, film grain, lofi aesthetic, muted warm palette"
  music_prompt: "ambient lofi study music, soft piano, gentle rain, warm analog tape hiss, mellow pads, no vocals, 70 bpm, calming, focus-enhancing"
  mood: relaxed-focus
  duration_minutes: 120
  resolution: "1080p"
  quality_preset: high
  scene_count: 24
  scene_duration_seconds: 25.0
  enable_weather: true
  enable_time_progression: true
  enable_parallax: true
  enable_particles: true
  parallax_strength: 0.03

post:
  watermark_enabled: false
  subtitles_enabled: false
  intro_enabled: false
  outro_enabled: false

publish:
  youtube_enabled: false
  youtube_title: "Study With Me — Lofi Chill Session"
  youtube_description: "A relaxed lofi study session to help you focus and get work done."
  youtube_tags:
    - study with me
    - lofi
    - focus music
    - chill
    - productivity
  youtube_category_id: "27"
  youtube_privacy: private
  thumbnail_enabled: true

notify:
  discord_webhook_url: null
  slack_webhook_url: null
  require_approval: true
  approval_timeout_seconds: 3600
```

**config/profiles/tech_tutorial.yaml**
```yaml
profile: tech-tutorial

video:
  style_prompt: "clean modern office, bright white background, professional lighting, shallow depth of field, tech aesthetic, blue accent colors"
  music_prompt: "upbeat background music, light electronic, no distracting vocals, modern corporate, motivating, 100 bpm"
  mood: professional
  duration_minutes: 10
  resolution: "1080p"
  quality_preset: high
  scene_count: 8
  scene_duration_seconds: 15.0
  enable_weather: false
  enable_time_progression: false
  enable_parallax: true
  enable_particles: false
  parallax_strength: 0.02

post:
  watermark_enabled: true
  watermark_text: "@TechAIHacks"
  watermark_position: bottom-right
  subtitles_enabled: true
  subtitles_srt_path: ""
  intro_enabled: false
  outro_enabled: false

publish:
  youtube_enabled: false
  youtube_title: "How to Use AI Tools to Automate Your Workflow"
  youtube_description: "Step-by-step tutorial on using AI tools to automate repetitive tasks and boost productivity."
  youtube_tags:
    - AI tutorial
    - automation
    - tech tips
    - productivity
    - how to
  youtube_category_id: "28"
  youtube_privacy: private
  thumbnail_enabled: true

notify:
  discord_webhook_url: null
  slack_webhook_url: null
  require_approval: true
  approval_timeout_seconds: 3600
```

**config/profiles/cinematic.yaml**
```yaml
profile: cinematic

video:
  style_prompt: "cinematic wide shot, dramatic lighting, film photography, high contrast, warm golden hour, shallow depth of field, 35mm grain, premium production"
  music_prompt: "orchestral cinematic score, sweeping strings, subtle percussion, emotional build, no lyrics, Hollywood quality, 80 bpm"
  mood: dramatic
  duration_minutes: 60
  resolution: "1080p"
  quality_preset: high
  scene_count: 18
  scene_duration_seconds: 30.0
  enable_weather: true
  enable_time_progression: true
  enable_parallax: true
  enable_particles: true
  parallax_strength: 0.05

post:
  watermark_enabled: true
  watermark_text: "Your Channel"
  watermark_position: bottom-right
  subtitles_enabled: false
  intro_enabled: false
  outro_enabled: false

publish:
  youtube_enabled: false
  youtube_title: "Cinematic Study Session — Focus & Flow"
  youtube_description: "An immersive cinematic study session with dramatic visuals and orchestral music."
  youtube_tags:
    - cinematic
    - study with me
    - focus
    - orchestral
    - dramatic
  youtube_category_id: "27"
  youtube_privacy: private
  thumbnail_enabled: true

notify:
  discord_webhook_url: null
  slack_webhook_url: null
  require_approval: true
  approval_timeout_seconds: 3600
```

After writing all three files, validate each against PipelineConfig by running:
```bash
python -c "
from config import PipelineConfig
for name in ['lofi_study', 'tech_tutorial', 'cinematic']:
    cfg = PipelineConfig.from_yaml(f'config/profiles/{name}.yaml')
    print(f'{name}: OK — profile={cfg.profile}, mood={cfg.video.mood}')
"
```
All three must print `OK` with no ValidationError.
  </action>
  <verify>
```bash
python -c "
from config import PipelineConfig
for name in ['lofi_study', 'tech_tutorial', 'cinematic']:
    cfg = PipelineConfig.from_yaml(f'config/profiles/{name}.yaml')
    print(f'{name}: profile={cfg.profile}, mood={cfg.video.mood}, youtube_enabled={cfg.publish.youtube_enabled}')
"
```
Expected: three lines printed, all with `youtube_enabled=False`, different `mood` values.
  </verify>
  <done>
All three YAML files exist under config/profiles/. Each loads cleanly via PipelineConfig.from_yaml() with no ValidationError. Profile names, prompts, and moods differ meaningfully between profiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore to protect credentials and config secrets</name>
  <files>.gitignore</files>
  <action>
Read the existing .gitignore file first. If it does not exist, create it from scratch. Append (do NOT replace existing content) the following block at the end of the file. Separate from any existing content with a blank line and a comment header:

```
# ---- Content Creation Toolkit — Shared Layer ----
# Credentials and secrets (never commit)
credentials/
.secrets/
*.pickle
client_secrets.json
youtube_token.json
token.json
.env
.env.*
!.env.example

# User-saved run configs (gitignore but keep .gitkeep)
configs/*
!configs/.gitkeep

# Approval gate marker files
*.approved

# Generated output directories (large binary files)
out/
tmp/
generated/
```

Also create placeholder directories with .gitkeep files so the directories exist in the repo:
- `credentials/.gitkeep`
- `configs/.gitkeep`

Use `mkdir -p credentials configs` then write empty `.gitkeep` files to each. Do NOT create a `credentials/client_secrets.json` or any actual credential file.

After writing .gitignore, verify git respects the new rules:
```bash
git check-ignore -v credentials/some_token.json
```
Expected: shows the `.gitignore` rule that would ignore it.
  </action>
  <verify>
```bash
git check-ignore -v credentials/ configs/
```
Both paths should show as ignored. Also check:
```bash
git status --short credentials/ configs/
```
The .gitkeep files should appear as untracked (since the directories themselves are gitignored but .gitkeep may need `!credentials/.gitkeep` exception — add it if needed so the empty dir is tracked).

Adjust: If `.gitkeep` files are being ignored by `credentials/` rule, add explicit exceptions:
```
!credentials/.gitkeep
!configs/.gitkeep
```
Re-verify after adding exceptions.
  </verify>
  <done>
.gitignore updated. credentials/ and configs/ directories exist with .gitkeep files. Running `git check-ignore -v credentials/token.json` shows the file would be ignored. .gitkeep files are tracked or at minimum the directories are committed.
  </done>
</task>

</tasks>

<verification>
Run the full validation sequence:
```bash
python -c "
from config import PipelineConfig
profiles = ['lofi_study', 'tech_tutorial', 'cinematic']
for p in profiles:
    cfg = PipelineConfig.from_yaml(f'config/profiles/{p}.yaml')
    assert cfg.profile != 'default', f'{p} did not override profile name'
    print(f'PASS: {p} — {cfg.video.mood}')
print('All profiles valid.')
"
```

Check gitignore:
```bash
git check-ignore -v credentials/ .env configs/run.yaml
```
All three should show as ignored.
</verification>

<success_criteria>
- config/profiles/ directory contains exactly three YAML files: lofi_study.yaml, tech_tutorial.yaml, cinematic.yaml
- Each file loads without ValidationError via PipelineConfig.from_yaml()
- Each profile has distinct video.style_prompt, video.mood, and video.music_prompt values
- .gitignore prevents credentials/, configs/ content, .env, and *.pickle from being committed
- credentials/.gitkeep and configs/.gitkeep exist to anchor directory structure in git
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-foundation/01-02-SUMMARY.md` following the summary template.
</output>
