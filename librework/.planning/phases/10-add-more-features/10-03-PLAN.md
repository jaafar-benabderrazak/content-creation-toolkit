---
phase: 10-add-more-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - librework/backend/app/api/v1/owner.py
  - librework/frontend/src/components/OwnerDashboard.tsx
  - librework/frontend/src/components/ExplorePage.tsx
autonomous: true
requirements: [OWNER-01, SEARCH-01]

must_haves:
  truths:
    - "Owner dashboard displays revenue chart with real time-series data from reservations"
    - "Owner dashboard shows occupancy rate and booking trend charts"
    - "Explore page centers map on user's geolocation when permission is granted"
  artifacts:
    - path: "librework/backend/app/api/v1/owner.py"
      provides: "Time-series analytics endpoints for revenue and occupancy"
      contains: "analytics"
    - path: "librework/frontend/src/components/OwnerDashboard.tsx"
      provides: "Enhanced dashboard with recharts LineChart and BarChart"
      contains: "recharts"
  key_links:
    - from: "librework/frontend/src/components/OwnerDashboard.tsx"
      to: "/api/v1/owner/analytics"
      via: "fetch or axios call for chart data"
      pattern: "owner/analytics"
    - from: "librework/frontend/src/components/ExplorePage.tsx"
      to: "navigator.geolocation"
      via: "browser Geolocation API"
      pattern: "navigator\\.geolocation"
---

<objective>
Enhance owner analytics dashboard with real time-series charts and add user-location centering to the Explore map.

Purpose: Owners see actual business performance trends (not just aggregate stats). Users get a map centered on their location for better space discovery.
Output: Extended owner analytics API, recharts-powered dashboard, geolocation-aware Explore page.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-more-features/10-RESEARCH.md

@librework/backend/app/api/v1/owner.py
@librework/frontend/src/components/OwnerDashboard.tsx
@librework/frontend/src/components/ExplorePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add time-series analytics endpoints and enhance OwnerDashboard</name>
  <files>
    librework/backend/app/api/v1/owner.py
    librework/frontend/src/components/OwnerDashboard.tsx
  </files>
  <action>
    1. In `owner.py`, add two new endpoints:

       - `GET /owner/analytics/revenue?period=week|month|quarter`: Query reservations for the owner's establishments, group by date (using Python datetime grouping on `created_at` or `start_time`), compute daily revenue totals. Return `[{"date": "2026-03-15", "revenue": 450}, ...]`. Filter by period: week = last 7 days, month = last 30, quarter = last 90. Use `Depends(get_current_owner)`.

       - `GET /owner/analytics/occupancy?period=week|month`: For each day in the period, compute occupancy = (number of confirmed reservations) / (total spaces * available hours estimate). Return `[{"date": "2026-03-15", "occupancy": 0.72}, ...]`. Simplified v1: occupancy = confirmed_reservations_count / total_spaces for that day. Use `Depends(get_current_owner)`.

    2. In `OwnerDashboard.tsx` (already 'use client'):
       - Import `LineChart, Line, BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend` from recharts (already installed).
       - Add state for `revenueData` and `occupancyData` (fetched from the new endpoints).
       - Add a period selector (week/month/quarter) using existing shadcn Select or Tabs component.
       - Render a `LineChart` for revenue trends (line color #F9AB18 to match brand).
       - Render a `BarChart` for occupancy rates (bar color #F9AB18).
       - Place charts below the existing stats cards in a 2-column grid (lg:grid-cols-2, grid-cols-1 on mobile).
       - Fetch data via `api.get('/owner/analytics/revenue?period=week')` using the existing axios instance from `@/lib/api`.
       - Handle loading and error states gracefully (skeleton or "No data" message).

    IMPORTANT: All recharts components must remain in a 'use client' component. The OwnerDashboard.tsx already has 'use client' so this is safe.
  </action>
  <verify>
    Run `cd librework/backend && python -c "from app.api.v1.owner import router; routes = [r.path for r in router.routes]; print(routes); assert '/analytics/revenue' in routes or any('analytics' in r for r in routes)"`.
    Run `cd librework/frontend && npx tsc --noEmit 2>&1 | grep -i "OwnerDashboard" || echo "No type errors in OwnerDashboard"`.
  </verify>
  <done>Owner dashboard shows revenue LineChart and occupancy BarChart with real data from backend. Period selector toggles between week/month/quarter views. Two new analytics endpoints exist in owner.py.</done>
</task>

<task type="auto">
  <name>Task 2: Add user-location centering to Explore map</name>
  <files>
    librework/frontend/src/components/ExplorePage.tsx
  </files>
  <action>
    1. In `ExplorePage.tsx`, add a `useEffect` that runs once on mount:
       - Check `navigator.geolocation` availability.
       - Call `navigator.geolocation.getCurrentPosition(successCallback, errorCallback, { enableHighAccuracy: false, timeout: 5000 })`.
       - On success: update the map center state to the user's coordinates `{ lat, lng }`.
       - On error or denial: keep the existing default center (Paris or whatever is currently set).
       - Add a "Center on me" button (use lucide-react `LocateFixed` icon) positioned in the top-right of the map area. On click, re-trigger geolocation and recenter.

    2. The map component uses `@vis.gl/react-google-maps` (APIProvider + Map from Phase 9). Update the `Map` component's `center` prop to use the user-location state when available. Use `defaultCenter` as fallback.

    Do NOT add search-by-address (text input geocoding) in this plan — that is a v2 feature. Keep this to geolocation centering only.
  </action>
  <verify>
    Run `cd librework/frontend && npx tsc --noEmit 2>&1 | grep -i "ExplorePage" || echo "No type errors in ExplorePage"`.
    Grep for `navigator.geolocation` in ExplorePage.tsx to confirm integration.
  </verify>
  <done>Explore page requests user location on mount and centers map accordingly. "Center on me" button re-centers. Graceful fallback to default center when geolocation is denied or unavailable.</done>
</task>

</tasks>

<verification>
- Owner analytics endpoints return time-series data arrays
- OwnerDashboard renders recharts LineChart and BarChart
- ExplorePage uses navigator.geolocation for map centering
- No SSR errors from recharts (already in 'use client' component)
- Period selector works in dashboard
</verification>

<success_criteria>
- Owner sees revenue and occupancy charts with real reservation data
- Charts update when period selector changes
- Explore map centers on user location when permission granted
- All components compile without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-more-features/10-03-SUMMARY.md`
</output>
