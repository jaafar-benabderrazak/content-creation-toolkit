---
phase: 10-add-more-features
plan: 06
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03, 10-04, 10-05]
files_modified:
  - librework/frontend/e2e/stripe-checkout.spec.ts
  - librework/frontend/e2e/email-notifications.spec.ts
  - librework/frontend/e2e/i18n-locale.spec.ts
  - librework/frontend/e2e/owner-analytics.spec.ts
  - librework/frontend/e2e/explore-geolocation.spec.ts
  - librework/frontend/playwright.config.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, EMAIL-01, I18N-01, I18N-02, OWNER-01, SEARCH-01]

must_haves:
  truths:
    - "Playwright E2E tests exist for all Phase 10 features"
    - "Tests can run via npx playwright test and produce a report"
    - "Tests cover: Stripe checkout flow, i18n locale switching, owner analytics charts, explore geolocation"
  artifacts:
    - path: "librework/frontend/playwright.config.ts"
      provides: "Playwright configuration for E2E tests"
      contains: "defineConfig"
    - path: "librework/frontend/e2e/i18n-locale.spec.ts"
      provides: "i18n locale routing and switching tests"
      contains: "test"
    - path: "librework/frontend/e2e/stripe-checkout.spec.ts"
      provides: "Stripe checkout flow tests"
      contains: "test"
  key_links:
    - from: "librework/frontend/e2e/*.spec.ts"
      to: "librework/frontend/playwright.config.ts"
      via: "Playwright test runner"
      pattern: "playwright"
---

<objective>
Create Playwright E2E tests covering all Phase 10 features: Stripe checkout, email notifications, i18n locale switching, owner analytics, and explore geolocation.

Purpose: Automated verification that all Phase 10 features work end-to-end in a browser. Catches regressions before deployment.
Output: Playwright config, 5 E2E test files, runnable test suite.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-more-features/10-RESEARCH.md
@.planning/phases/10-add-more-features/10-01-SUMMARY.md
@.planning/phases/10-add-more-features/10-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Playwright and create config</name>
  <files>
    librework/frontend/playwright.config.ts
  </files>
  <action>
    1. Run `cd librework/frontend && npm install --save-dev @playwright/test && npx playwright install chromium`.

    2. Create `playwright.config.ts`:
       - Import `defineConfig, devices` from '@playwright/test'.
       - Set `testDir: './e2e'`.
       - Set `baseURL: 'http://localhost:3000'`.
       - Set `webServer: { command: 'npm run dev', port: 3000, reuseExistingServer: !process.env.CI }`.
       - Configure projects: `[{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]`.
       - Set `retries: process.env.CI ? 2 : 0`.
       - Set `reporter: 'html'`.
       - Set reasonable timeouts: `timeout: 30000`, `expect: { timeout: 5000 }`.

    3. Create `e2e/` directory.
  </action>
  <verify>
    Run `ls librework/frontend/playwright.config.ts librework/frontend/e2e/` — both must exist.
  </verify>
  <done>Playwright installed and configured with chromium, e2e test directory created, webServer config points to dev server.</done>
</task>

<task type="auto">
  <name>Task 2: Write E2E test specs for all Phase 10 features</name>
  <files>
    librework/frontend/e2e/stripe-checkout.spec.ts
    librework/frontend/e2e/i18n-locale.spec.ts
    librework/frontend/e2e/owner-analytics.spec.ts
    librework/frontend/e2e/explore-geolocation.spec.ts
    librework/frontend/e2e/email-notifications.spec.ts
  </files>
  <action>
    1. Create `e2e/i18n-locale.spec.ts`:
       - Test "redirects root to /en": Navigate to '/', expect URL to contain '/en'.
       - Test "locale switcher changes to French": Navigate to '/en', click language switcher, expect URL to contain '/fr', expect page text to be in French (check Navbar text).
       - Test "French locale renders French content": Navigate to '/fr', check Navbar links contain French text from fr.json.
       - Test "API routes not locale-prefixed": Navigate to '/api/stripe/webhook' via page.goto, expect NOT to be redirected to '/en/api/...'.
       - Test "login page works under locale": Navigate to '/en/login', expect login form to render.

    2. Create `e2e/stripe-checkout.spec.ts`:
       - Test "checkout button exists on pending reservation": Mock or navigate to user dashboard with a pending reservation. Assert "Pay" button is visible.
       - Test "checkout redirects to Stripe": This is a smoke test — click Pay, expect navigation attempt (may need to mock the checkout API response to avoid real Stripe redirect). Use `page.route('**/api/stripe/checkout', ...)` to intercept and return mock `{ checkout_url: 'https://checkout.stripe.com/test' }`. Assert `page.url()` changes or navigation event fires.
       - Note: Full Stripe payment testing requires Stripe test mode and is beyond E2E scope. Focus on verifying the frontend flow initiates correctly.

    3. Create `e2e/owner-analytics.spec.ts`:
       - Test "analytics charts render on owner dashboard": Navigate to owner dashboard (may need auth mock). Assert that chart containers exist (look for recharts SVG elements or container divs).
       - Test "period selector changes data": If accessible, click period toggle and verify chart re-renders.
       - Note: These tests may need authenticated user state. Use `page.route` to mock API responses if real backend is not running.

    4. Create `e2e/explore-geolocation.spec.ts`:
       - Test "geolocation button exists": Navigate to explore page, assert "Center on me" button is visible.
       - Test "map renders with markers": Assert map container and marker elements exist.
       - Mock geolocation: Use `page.context().grantPermissions(['geolocation'])` and `page.context().setGeolocation({ latitude: 48.8566, longitude: 2.3522 })` before navigation.

    5. Create `e2e/email-notifications.spec.ts`:
       - Test "email is mentioned in booking confirmation UI": This is a UI-level test — after booking, check for confirmation message that mentions email. Since actual email sending requires backend + Resend, this test verifies the UI acknowledges email will be sent.
       - Keep this file minimal — email delivery is better tested via backend integration tests.

    All tests should use descriptive test names. Use `test.describe` blocks to group related tests. Use `page.route` for API mocking where real backend is unavailable.
  </action>
  <verify>
    Run `cd librework/frontend && npx tsc --noEmit 2>&1 | grep -i "e2e" || echo "No type errors in E2E files"`.
    Run `ls librework/frontend/e2e/*.spec.ts | wc -l` — should show 5 files.
  </verify>
  <done>5 E2E test spec files cover i18n locale routing, Stripe checkout flow, owner analytics charts, explore geolocation, and email notification UI. All specs compile without type errors. Tests use API mocking for backend-dependent flows.</done>
</task>

</tasks>

<verification>
- Playwright config exists with correct testDir and webServer
- 5 spec files exist in e2e/ directory
- i18n tests verify locale routing and switching
- Stripe tests verify checkout initiation flow
- All specs compile with TypeScript
- `npx playwright test --list` shows discovered tests
</verification>

<success_criteria>
- `npx playwright test --list` discovers tests from all 5 spec files
- Tests can run (with dev server) via `npx playwright test`
- Test report generated via `npx playwright show-report`
- No TypeScript compilation errors in test files
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-more-features/10-06-SUMMARY.md`
</output>
