---
phase: 10-add-more-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - librework/backend/app/core/email.py
  - librework/backend/app/api/v1/email.py
  - librework/backend/app/api/v1/reservations.py
  - librework/backend/app/main.py
  - librework/backend/app/core/config.py
autonomous: true
requirements: [EMAIL-01, EMAIL-02, EMAIL-03]

must_haves:
  truths:
    - "Booking confirmation email is sent via Resend when a reservation is created"
    - "Cancellation email is sent via Resend when a reservation is cancelled"
    - "Marketing email can be sent to users with marketing_opt_in=true"
  artifacts:
    - path: "librework/backend/app/core/email.py"
      provides: "Resend email helper functions"
      contains: "resend.Emails.send"
    - path: "librework/backend/app/api/v1/email.py"
      provides: "Marketing email endpoint"
      exports: ["router"]
  key_links:
    - from: "librework/backend/app/api/v1/reservations.py"
      to: "librework/backend/app/core/email.py"
      via: "send_booking_confirmation call after reservation creation"
      pattern: "send_booking_confirmation"
    - from: "librework/backend/app/core/email.py"
      to: "resend"
      via: "Resend Python SDK"
      pattern: "resend\\.Emails\\.send"

user_setup:
  - service: resend
    why: "Transactional and marketing email delivery"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: RESEND_FROM_EMAIL
        source: "Use 'onboarding@resend.dev' for testing, or verified domain email for production"
    dashboard_config:
      - task: "Verify sending domain (production only)"
        location: "Resend Dashboard -> Domains -> Add Domain"
---

<objective>
Integrate Resend email delivery for transactional emails (booking confirmation, cancellation) and marketing emails to opted-in users.

Purpose: Replace console-printed notifications with real email delivery. Users receive actionable emails for their reservations.
Output: Email helper module, marketing email endpoint, transactional email calls in reservation handlers.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-more-features/10-RESEARCH.md

@librework/backend/app/api/v1/reservations.py
@librework/backend/app/core/config.py
@librework/backend/app/core/email.py
@librework/backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Resend email helper and config</name>
  <files>
    librework/backend/app/core/email.py
    librework/backend/app/core/config.py
  </files>
  <action>
    1. Add to `config.py` Settings class:
       - `RESEND_API_KEY: str = ""`
       - `RESEND_FROM_EMAIL: str = "LibreWork <onboarding@resend.dev>"`

    2. Rewrite `backend/app/core/email.py` (currently may exist with placeholder code). Create these functions:

       - `send_booking_confirmation(to_email: str, user_name: str, space_name: str, start_time: str, end_time: str) -> dict | None`: Sets `resend.api_key`, sends HTML email with booking details (space name, date/time, confirmation message). Returns Resend response or None if RESEND_API_KEY is empty (graceful degradation for dev without key). Use try/except to catch resend errors and log them rather than crashing the reservation flow.

       - `send_cancellation_email(to_email: str, user_name: str, space_name: str, start_time: str) -> dict | None`: Similar pattern, sends cancellation notification.

       - `send_marketing_email(to_emails: list[str], subject: str, html_content: str) -> dict | None`: Sends to multiple recipients. Uses `resend.Emails.send` with `to` as the email list.

    HTML templates: Use simple inline HTML (not Jinja2 for v1). Include LibreWork branding (golden #F9AB18 accent color). Keep templates minimal but professional.

    IMPORTANT: All email functions must be non-blocking to the caller. If RESEND_API_KEY is empty string, log a warning and return None silently. Never let email failure crash a reservation operation.
  </action>
  <verify>
    Run `cd librework/backend && python -c "from app.core.email import send_booking_confirmation, send_cancellation_email, send_marketing_email; print('Email helpers loaded')"`.
    Run `cd librework/backend && python -c "from app.core.config import Settings; s = Settings.model_fields; assert 'RESEND_API_KEY' in s; print('Config OK')"`.
  </verify>
  <done>Three email helper functions exist. All degrade gracefully when RESEND_API_KEY is empty. Config has RESEND_API_KEY and RESEND_FROM_EMAIL.</done>
</task>

<task type="auto">
  <name>Task 2: Wire transactional emails into reservations and create marketing endpoint</name>
  <files>
    librework/backend/app/api/v1/reservations.py
    librework/backend/app/api/v1/email.py
    librework/backend/app/main.py
  </files>
  <action>
    1. In `reservations.py`:
       - In the `create_reservation` endpoint (POST handler): After successful reservation insertion into Supabase, call `send_booking_confirmation(current_user.email, current_user.name or current_user.email, space_name, start_time_str, end_time_str)`. Import from `app.core.email`. Wrap in try/except so email failure never blocks reservation creation. Fetch space name from the spaces table join if not already available.
       - In the cancellation endpoint (if exists, or the PATCH/DELETE that sets status to "cancelled"): Call `send_cancellation_email(...)` after successful status update. Same try/except pattern.

    2. Create `backend/app/api/v1/email.py`:
       - `router = APIRouter(prefix="/email", tags=["Email"])`
       - `POST /marketing/send`: Admin-only endpoint (use `Depends(get_current_admin)` or check role == "admin"). Accepts `subject: str`, `html_content: str` in request body. Queries Supabase `users` table for users with `marketing_opt_in = true` (if column exists) or all users (v1 — add marketing_opt_in column later). Calls `send_marketing_email(emails, subject, html_content)`. Returns count of recipients.

    3. Register email router in `main.py`: `app.include_router(email.router, prefix=f"{settings.API_V1_PREFIX}")`.
  </action>
  <verify>
    Run `cd librework/backend && python -c "from app.api.v1.email import router; print('Email router loaded:', len(router.routes), 'routes')"`.
    Grep for `send_booking_confirmation` in reservations.py to confirm wiring.
  </verify>
  <done>Reservation creation triggers booking confirmation email. Cancellation triggers cancellation email. Admin can send marketing emails via POST /email/marketing/send. Email router registered in main.py.</done>
</task>

</tasks>

<verification>
- `email.py` helper has 3 functions that gracefully handle missing API key
- `reservations.py` calls `send_booking_confirmation` after create
- `reservations.py` calls `send_cancellation_email` after cancel
- Email router has marketing send endpoint with admin-only access
- No email failure can crash a reservation operation (try/except wrapping)
</verification>

<success_criteria>
- Transactional emails fire on reservation create and cancel (with Resend key configured)
- Marketing email endpoint exists and is admin-restricted
- All email operations degrade gracefully without RESEND_API_KEY
- Email router registered in main.py
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-more-features/10-02-SUMMARY.md`
</output>
