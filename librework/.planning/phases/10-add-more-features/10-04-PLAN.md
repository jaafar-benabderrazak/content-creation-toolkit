---
phase: 10-add-more-features
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - librework/frontend/src/components/ReservationCheckout.tsx
  - librework/frontend/src/components/PaymentHistory.tsx
  - librework/frontend/src/app/page.tsx
  - librework/frontend/src/components/UserDashboard.tsx
autonomous: true
requirements: [PAY-04]

must_haves:
  truths:
    - "User can click Pay on a pending reservation and be redirected to Stripe Checkout"
    - "User can view their payment history showing confirmed/completed reservations"
    - "After successful Stripe payment, user is redirected to a success page"
  artifacts:
    - path: "librework/frontend/src/components/ReservationCheckout.tsx"
      provides: "Pay button that initiates Stripe Checkout redirect"
      contains: "checkout"
    - path: "librework/frontend/src/components/PaymentHistory.tsx"
      provides: "Payment history list component"
      contains: "payment"
  key_links:
    - from: "librework/frontend/src/components/ReservationCheckout.tsx"
      to: "/api/v1/payments/checkout-session"
      via: "POST request via axios api instance"
      pattern: "api\\.post.*checkout-session"
    - from: "librework/frontend/src/components/PaymentHistory.tsx"
      to: "/api/v1/payments/history"
      via: "GET request for payment records"
      pattern: "payments/history"
---

<objective>
Build the frontend checkout flow and payment history view that connects to the Stripe backend from Plan 01.

Purpose: Users can pay for reservations via Stripe Checkout and review their payment history.
Output: ReservationCheckout component, PaymentHistory component, integrated into existing dashboard.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-add-more-features/10-RESEARCH.md
@.planning/phases/10-add-more-features/10-01-SUMMARY.md

@librework/frontend/src/components/UserDashboard.tsx
@librework/frontend/src/app/page.tsx
@librework/frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReservationCheckout component with Stripe redirect</name>
  <files>
    librework/frontend/src/components/ReservationCheckout.tsx
  </files>
  <action>
    1. Create `ReservationCheckout.tsx` ('use client' component):
       - Props: `reservationId: string`, `spaceName: string`, `amount: number` (in credits, for display), `onCancel: () => void`.
       - Displays reservation summary: space name, amount in credits/EUR.
       - "Pay Now" button (shadcn Button, primary color).
       - On click: use the axios `api` instance from `@/lib/api` to call `api.post('/payments/checkout-session', { reservation_id: reservationId })`. This sends the Bearer token automatically (same pattern as PaymentHistory in Task 2). On success, `window.location.href = response.data.checkout_url` to redirect to Stripe hosted checkout. On error, show toast error via sonner or simple alert.
       - Do NOT use raw `fetch()` — the FastAPI backend requires a Bearer token via `get_current_user`, and only the axios `api` instance from `@/lib/api` attaches it. Using raw fetch with `credentials: 'include'` will result in 401.
       - Loading state while checkout session is being created (disable button, show spinner).
       - "Cancel" button calls `onCancel` prop.

    2. Do NOT use `@stripe/stripe-js` `redirectToCheckout` — that is deprecated. Use direct `window.location.href = session.url` redirect pattern per research.

    3. Style with Tailwind: card layout with shadow, centered content, clear CTA button.
  </action>
  <verify>
    Run `cd librework/frontend && npx tsc --noEmit 2>&1 | grep -i "ReservationCheckout" || echo "No type errors"`.
  </verify>
  <done>ReservationCheckout component renders reservation summary with Pay Now button that calls /payments/checkout-session via axios api instance and redirects to Stripe Checkout via session URL.</done>
</task>

<task type="auto">
  <name>Task 2: Create PaymentHistory component and integrate into UserDashboard</name>
  <files>
    librework/frontend/src/components/PaymentHistory.tsx
    librework/frontend/src/components/UserDashboard.tsx
  </files>
  <action>
    1. Create `PaymentHistory.tsx` ('use client' component):
       - Fetches from `/api/v1/payments/history` via the axios api instance from `@/lib/api`.
       - Renders a table or card list of payment records: space name, date, amount, status (confirmed/completed).
       - Empty state: "No payments yet" message.
       - Loading state: skeleton or spinner.
       - Each row shows formatted date (use date-fns `format`), amount in EUR (credits / 100 or display as credits).

    2. In `UserDashboard.tsx`:
       - Add a "Payment History" tab or section (use existing shadcn Tabs if the dashboard already uses tabs, otherwise add a section below reservations).
       - Import and render `<PaymentHistory />` in the appropriate location.
       - Add a "Pay" button next to each pending reservation in the reservations list. On click, show the `ReservationCheckout` component (modal or inline). Import `ReservationCheckout` and manage show/hide state.

    3. Wire the Pay button: when user clicks Pay on a pending reservation, render `<ReservationCheckout reservationId={id} spaceName={name} amount={cost_credits} onCancel={() => setShowCheckout(false)} />`.
  </action>
  <verify>
    Run `cd librework/frontend && npx tsc --noEmit 2>&1 | grep -i "PaymentHistory\|UserDashboard" || echo "No type errors"`.
  </verify>
  <done>PaymentHistory component displays payment records. UserDashboard has Pay button on pending reservations that opens ReservationCheckout. Payment History section visible in dashboard.</done>
</task>

</tasks>

<verification>
- ReservationCheckout calls /payments/checkout-session via axios api instance (NOT raw fetch)
- PaymentHistory fetches from /payments/history endpoint via axios api instance
- UserDashboard shows Pay button on pending reservations
- All components are 'use client' and compile cleanly
</verification>

<success_criteria>
- User can click Pay on a pending reservation and reach Stripe Checkout
- User can view payment history in their dashboard
- Components handle loading and error states
- No type errors in any modified files
</success_criteria>

<output>
After completion, create `.planning/phases/10-add-more-features/10-04-SUMMARY.md`
</output>