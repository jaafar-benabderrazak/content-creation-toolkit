---
phase: 09-fix-select-visibility-issues-and-integrate-google-maps-open-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - librework/frontend/package.json
  - librework/frontend/src/lib/mockData.ts
  - librework/frontend/src/lib/googlePlaces.ts
  - librework/frontend/src/components/providers.tsx
  - librework/frontend/src/components/MapView.tsx
  - librework/frontend/src/components/ExplorePage.tsx
autonomous: false
requirements:
  - SEARCH-01
user_setup:
  - service: google-maps
    why: "Google Maps JavaScript API + Places API (New) for real establishment data"
    env_vars:
      - name: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials -> Create API Key. Then restrict to HTTP referrers (localhost:3000 + your domain). Enable 'Maps JavaScript API' and 'Places API (New)' on the key."
    dashboard_config:
      - task: "Enable Maps JavaScript API"
        location: "Google Cloud Console -> APIs & Services -> Library -> search 'Maps JavaScript API' -> Enable"
      - task: "Enable Places API (New)"
        location: "Google Cloud Console -> APIs & Services -> Library -> search 'Places API (New)' -> Enable"

must_haves:
  truths:
    - "User sees real establishments (cafes, coworking spaces, libraries) from Google Places on the Explore page"
    - "User sees establishment markers on the map at their real GPS coordinates"
    - "Clicking a marker or list item navigates to establishment details"
    - "Existing mock data still works as fallback when API key is not configured"
  artifacts:
    - path: "librework/frontend/src/lib/googlePlaces.ts"
      provides: "Places API fetch utility that returns Establishment[]"
      exports: ["fetchNearbyEstablishments"]
    - path: "librework/frontend/src/lib/mockData.ts"
      provides: "Establishment interface with optional coordinates field"
      contains: "coordinates?"
    - path: "librework/frontend/src/components/providers.tsx"
      provides: "APIProvider wrapper for Google Maps"
      contains: "APIProvider"
    - path: "librework/frontend/src/components/MapView.tsx"
      provides: "Map view using real coordinates from Establishment.coordinates"
      contains: "est.coordinates"
    - path: "librework/frontend/src/components/ExplorePage.tsx"
      provides: "Explore page fetching real places via googlePlaces.ts"
      contains: "fetchNearbyEstablishments"
  key_links:
    - from: "librework/frontend/src/components/ExplorePage.tsx"
      to: "librework/frontend/src/lib/googlePlaces.ts"
      via: "import and call fetchNearbyEstablishments"
      pattern: "fetchNearbyEstablishments"
    - from: "librework/frontend/src/lib/googlePlaces.ts"
      to: "google.maps.places.Place.searchNearby"
      via: "Maps JavaScript API loaded by APIProvider"
      pattern: "Place\\.searchNearby"
    - from: "librework/frontend/src/components/MapView.tsx"
      to: "librework/frontend/src/lib/mockData.ts"
      via: "Establishment.coordinates for marker placement"
      pattern: "est\\.coordinates"
---

<objective>
Integrate Google Maps Places API (New) to display real establishments (cafes, coworking spaces, libraries) on the Explore page map and list view, replacing hardcoded mock data with live nearby search results.

Purpose: Users currently see 5 hardcoded fake Paris establishments. This plan connects to Google Places to show real venues with real coordinates, ratings, photos, and addresses.
Output: Working Places API integration with real map markers, fallback to mock data when API key is absent.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-fix-select-visibility-issues-and-integrate-google-maps-open-data/09-RESEARCH.md
@librework/frontend/src/lib/mockData.ts
@librework/frontend/src/components/providers.tsx
@librework/frontend/src/components/MapView.tsx
@librework/frontend/src/components/ExplorePage.tsx
@librework/frontend/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, extend Establishment interface, create Places fetch utility</name>
  <files>
    librework/frontend/package.json
    librework/frontend/src/lib/mockData.ts
    librework/frontend/src/lib/googlePlaces.ts
  </files>
  <action>
1. Install packages:
   ```
   cd librework/frontend && npm install @vis.gl/react-google-maps @types/google.maps
   ```

2. Add optional `coordinates` field to the `Establishment` interface in `mockData.ts`:
   ```typescript
   export interface Establishment {
     // ... all existing fields unchanged ...
     coordinates?: { lat: number; lng: number }  // Real GPS from Places API
   }
   ```
   Do NOT remove or rename any existing fields. Only add `coordinates` as optional.
   Add coordinates to each of the 5 existing mock establishments using the values from the existing `COORDS` record in MapView.tsx:
   - id '1': { lat: 48.8566, lng: 2.3522 }
   - id '2': { lat: 48.8484, lng: 2.3455 }
   - id '3': { lat: 48.8606, lng: 2.3376 }
   - id '4': { lat: 48.8530, lng: 2.3499 }
   - id '5': { lat: 48.8620, lng: 2.3510 }

3. Create `librework/frontend/src/lib/googlePlaces.ts`:
   - Export `fetchNearbyEstablishments(center: { lat: number; lng: number }, radiusMeters?: number): Promise<Establishment[]>`
   - Default radiusMeters = 2000
   - Use `google.maps.importLibrary('places')` to get the Place class (requires APIProvider to be mounted)
   - Search for place types: `cafe`, `coworking_space`, `library`
   - Request fields: `displayName`, `formattedAddress`, `location`, `rating`, `userRatingCount`, `photos`, `types`, `businessStatus`
   - Do NOT request `openingHours` (bumps to Pro SKU, saves cost)
   - Use `SearchNearbyRankPreference.POPULARITY`
   - `maxResultCount: 10` per place type
   - Map results to `Establishment` interface using a `mapPlaceToEstablishment` function:
     - `id`: `place.id ?? crypto.randomUUID()`
     - `name`: `place.displayName ?? 'Unknown'`
     - `category`: map `cafe` -> `'cafe'`, `coworking_space` -> `'coworking'`, `library` -> `'library'`
     - `image`: `place.photos?.[0]?.getURI({ maxWidth: 1080 }) ?? ''`
     - `rating`: `place.rating ?? 0`
     - `distance`: `''` (computed separately if needed)
     - `address`: `place.formattedAddress ?? ''`
     - `description`: `''` (not available from Places API)
     - `amenities`: `[]` (not available)
     - `spaces`: `[]` (LibreWork-specific, not from Google)
     - `reviews`: `[]` (not available from basic fields)
     - `coordinates`: `{ lat: place.location?.lat() ?? 0, lng: place.location?.lng() ?? 0 }`
   - Wrap the entire function body in try/catch, return `[]` on error and `console.error` the error
   - Add `'use client'` directive -- this module uses browser-only `google.maps` globals

  Follow the exact pattern from 09-RESEARCH.md Pattern 3.
  </action>
  <verify>
`cd librework/frontend && npx tsc --noEmit` -- TypeScript compilation succeeds with no errors related to mockData or googlePlaces.
  </verify>
  <done>
`@vis.gl/react-google-maps` and `@types/google.maps` are installed. `Establishment` interface has optional `coordinates` field. Mock establishments have coordinates populated. `googlePlaces.ts` exports `fetchNearbyEstablishments` that maps Google Places results to `Establishment[]`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add APIProvider to providers, wire ExplorePage and MapView to use real data</name>
  <files>
    librework/frontend/src/components/providers.tsx
    librework/frontend/src/components/ExplorePage.tsx
    librework/frontend/src/components/MapView.tsx
  </files>
  <action>
1. Update `providers.tsx`:
   - Import `APIProvider` from `@vis.gl/react-google-maps`
   - Wrap children in `APIProvider` only when `process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is defined:
     ```typescript
     const apiKey = process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
     // Inside the return:
     // If apiKey exists, wrap with <APIProvider apiKey={apiKey}>{children}</APIProvider>
     // Otherwise, just render {children} without APIProvider
     ```
   - This ensures the app works without a Google API key (falls back to mock data)

2. Update `ExplorePage.tsx`:
   - Import `fetchNearbyEstablishments` from `@/lib/googlePlaces`
   - Keep the existing `establishments` import from `mockData` as fallback
   - Add state: `const [liveEstablishments, setLiveEstablishments] = useState<Establishment[]>([])`
   - Add state: `const [isLoading, setIsLoading] = useState(false)`
   - Check `typeof window !== 'undefined' && !!process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` to decide whether to attempt Places fetch.
   - Add a `useEffect` that calls `fetchNearbyEstablishments({ lat: 48.8566, lng: 2.3522 })` (Paris center) when the component mounts, if API key is configured. Set `isLoading = true` before, `false` after. On success, set `liveEstablishments`. On error, fall back to mock data silently.
   - Use `liveEstablishments.length > 0 ? liveEstablishments : establishments` as the data source for filtering and rendering
   - Add a subtle loading state: when `isLoading` is true, show a spinner or "Loading nearby spaces..." text in place of the establishment list
   - Handle `spaces: []` gracefully: in the establishment card rendering, if `est.spaces.length === 0`, show a small badge "Contact to book" instead of space count
   - Add `useEffect` import if not already present

3. Update `MapView.tsx`:
   - Remove the hardcoded `COORDS` record entirely
   - In the marker creation loop, use `est.coordinates` instead of `COORDS[est.id]`:
     ```typescript
     establishments.forEach((est) => {
       const coords = est.coordinates
       if (!coords) return
       // Use [coords.lat, coords.lng] for marker position
       // ... rest of existing marker creation logic unchanged
     })
     ```
   - Keep everything else (Leaflet initialization, tile layer, custom icons, popup content) unchanged
   - The map should still work with mock data (which now has coordinates populated from Task 1)

Do NOT change the Establishment card layout, rating display, or navigation logic. Only change the data source and coordinate resolution.
  </action>
  <verify>
1. `cd librework/frontend && npm run build` -- build succeeds
2. `cd librework/frontend && npm run dev` -- start dev server
3. Without NEXT_PUBLIC_GOOGLE_MAPS_API_KEY set: Explore page shows 5 mock establishments with markers on map (using coordinates from mockData)
4. With NEXT_PUBLIC_GOOGLE_MAPS_API_KEY set: Explore page loads real nearby places from Google with markers at real GPS positions
  </verify>
  <done>
APIProvider conditionally wraps the app when API key is present. ExplorePage fetches real places from Google and falls back to mock data. MapView uses `est.coordinates` instead of hardcoded COORDS lookup. Build passes. App works with and without API key.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Google Maps Places integration end-to-end</name>
  <files>librework/frontend/src/components/ExplorePage.tsx</files>
  <action>
Human verifies the Google Maps Places integration works correctly in both modes (with and without API key).
  </action>
  <verify>
1. Start the dev server: `cd librework/frontend && npm run dev`
2. Open http://localhost:3000 in a browser
3. Navigate to the Explore page
4. WITHOUT API key: Verify 5 mock establishments appear in list and on map with markers
5. WITH API key (set NEXT_PUBLIC_GOOGLE_MAPS_API_KEY in .env.local):
   a. Reload the page
   b. Verify real cafes, coworking spaces, and libraries appear in the list
   c. Verify markers appear on the map at real locations
   d. Click a marker -- verify popup shows establishment name
   e. Click an establishment card -- verify navigation to details page works
6. Verify establishments with no spaces show "Contact to book" badge
  </verify>
  <done>User confirms the integration works correctly in both fallback and live modes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes in librework/frontend
2. TypeScript compilation clean (`npx tsc --noEmit`)
3. Without API key: mock data displays correctly with map markers
4. With API key: real Google Places data loads and displays
5. No console errors related to Google Maps or Places API
6. Establishment interface is backward-compatible (coordinates is optional)
</verification>

<success_criteria>
- Real establishments from Google Places appear on Explore page when API key is configured
- Map markers use real GPS coordinates from the Places API
- App degrades gracefully to mock data when no API key is present
- Establishment interface remains backward-compatible with all existing consumers
- Build passes with no TypeScript or runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-select-visibility-issues-and-integrate-google-maps-open-data/09-02-SUMMARY.md`
</output>
