---
phase: 01-auth-migration
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - librework/backend/app/api/v1/users.py
  - librework/backend/app/api/v1/reservations.py
  - librework/backend/app/api/v1/reviews.py
  - librework/backend/app/api/v1/spaces.py
  - librework/backend/app/api/v1/notifications.py
  - librework/backend/app/api/v1/owner.py
  - librework/backend/app/api/v1/loyalty.py
  - librework/backend/app/api/v1/establishments.py
  - librework/backend/app/api/v1/favorites.py
  - librework/backend/app/api/v1/groups.py
  - librework/backend/app/api/v1/credits.py
  - librework/backend/app/api/v1/calendar.py
  - librework/backend/app/api/v1/activity.py
  - librework/backend/app/api/v1/admin_audit.py
  - librework/backend/app/api/v1/rbac.py
  - librework/backend/app/main.py
  - librework/backend/app/core/auth_enhanced.py
  - librework/backend/app/core/security.py
  - librework/backend/app/core/auth_simple.py
  - librework/backend/app/api/v1/auth.py
  - librework/backend/app/api/v1/auth_enhanced.py
autonomous: true
requirements: [AUTH-05, AUTH-07]

must_haves:
  truths:
    - "All API routers import get_current_user from app.core.dependencies (the unified Stack Auth version)"
    - "No router imports from app.core.auth_enhanced or app.core.security"
    - "Legacy auth files are deleted from the codebase"
    - "Backend starts without import errors"
  artifacts:
    - path: "librework/backend/app/main.py"
      provides: "FastAPI app with legacy auth routers removed"
      contains: "include_router"
    - path: "librework/backend/app/api/v1/users.py"
      provides: "Users router using Stack Auth dependencies"
      contains: "from app.core.dependencies import"
  key_links:
    - from: "librework/backend/app/api/v1/*.py"
      to: "librework/backend/app/core/dependencies.py"
      via: "Depends(get_current_user)"
      pattern: "from app\\.core\\.dependencies import"
    - from: "librework/backend/app/main.py"
      to: "librework/backend/app/api/v1/*.py"
      via: "include_router()"
      pattern: "include_router"
---

<objective>
Migrate all backend API routers to the new Stack Auth dependencies and remove all legacy auth code.

Purpose: After Plan 01 created the new auth foundation, this plan wires every router to use it and deletes the old auth system. After this, the backend is fully on Stack Auth with zero legacy auth code.
Output: All 15+ routers updated, legacy auth files deleted, main.py cleaned up.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-migration/01-RESEARCH.md
@.planning/phases/01-auth-migration/01-01-SUMMARY.md

@librework/backend/app/main.py
@librework/backend/app/api/v1/users.py
@librework/backend/app/api/v1/reservations.py
@librework/backend/app/api/v1/owner.py
@librework/backend/app/api/v1/admin_audit.py
@librework/backend/app/api/v1/rbac.py
@librework/backend/app/api/v1/auth.py
@librework/backend/app/api/v1/auth_enhanced.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all router auth imports to unified dependency</name>
  <files>
    librework/backend/app/api/v1/users.py
    librework/backend/app/api/v1/reservations.py
    librework/backend/app/api/v1/reviews.py
    librework/backend/app/api/v1/spaces.py
    librework/backend/app/api/v1/notifications.py
    librework/backend/app/api/v1/owner.py
    librework/backend/app/api/v1/loyalty.py
    librework/backend/app/api/v1/establishments.py
    librework/backend/app/api/v1/favorites.py
    librework/backend/app/api/v1/groups.py
    librework/backend/app/api/v1/credits.py
    librework/backend/app/api/v1/calendar.py
    librework/backend/app/api/v1/activity.py
    librework/backend/app/api/v1/admin_audit.py
    librework/backend/app/api/v1/rbac.py
  </files>
  <action>
    Systematically update every router file in `backend/app/api/v1/`:

    **Step 1: Find all files that import auth dependencies.**
    Run grep to find all files importing from `auth_enhanced`, `security`, or `dependencies`:
    - `grep -rl "from app.core.auth_enhanced import" backend/app/api/v1/`
    - `grep -rl "from app.core.security import" backend/app/api/v1/`
    - `grep -rl "from app.core.dependencies import" backend/app/api/v1/`

    **Step 2: For each file found, update imports:**

    Files importing from `app.core.auth_enhanced` (expected: rbac.py router, admin_audit.py, auth_enhanced.py router):
    - Replace `from app.core.auth_enhanced import get_current_user` with `from app.core.dependencies import get_current_user`
    - Replace `from app.core.auth_enhanced import get_current_admin` with `from app.core.dependencies import get_current_admin`
    - If the file imports `authenticate_user`, `create_tokens`, or other auth_enhanced-specific functions, those are only in the auth routers being deleted — skip those files (they'll be deleted in Task 2)

    Files importing from `app.core.security` (expected: some routers may import `decode_access_token` or `security` scheme):
    - Replace with imports from `app.core.dependencies`
    - If a router directly uses `security = HTTPBearer()` + `Depends(security)` to extract the token, replace the entire pattern with `Depends(get_current_user)` which now handles token extraction internally

    Files importing from `app.core.dependencies` (expected: users.py, reservations.py, reviews.py, spaces.py, notifications.py, owner.py, loyalty.py, establishments.py, favorites.py, groups.py, credits.py, calendar.py, activity.py):
    - These already import from the right module — just verify the import still works with the rewritten dependencies.py
    - If they import `get_current_user` and it returns `UserResponse`, no changes needed (Plan 01 preserved the return type)
    - If any router accesses `current_user.id` — this should still work since UserResponse has `id` (UUID)
    - If any router accesses token-specific fields like `current_user["sub"]` (dict access) — these need to change to attribute access (`current_user.id`) since get_current_user now returns UserResponse consistently

    **Step 3: Handle the `rbac.py` router file:**
    - The rbac.py ROUTER (not core/rbac.py) may have endpoints for managing custom roles (create_custom_role, update_custom_role, etc.)
    - REMOVE all custom_role CRUD endpoints from the router — per user decision: 3 fixed roles, no custom_roles
    - Keep any role-checking or permission-listing endpoints that are useful for RBAC

    **Step 4: Verify no broken imports remain.**
    Run: `grep -r "from app.core.auth_enhanced import\|from app.core.security import" backend/app/api/v1/` — should return nothing (except auth.py and auth_enhanced.py which are deleted in Task 2).
  </action>
  <verify>
    1. `grep -r "from app.core.auth_enhanced import" librework/backend/app/api/v1/` returns matches ONLY in files being deleted (auth.py, auth_enhanced.py) or NO matches at all
    2. `grep -r "from app.core.security import" librework/backend/app/api/v1/` returns NO matches
    3. `python -c "import app.api.v1.users; import app.api.v1.reservations; import app.api.v1.owner; print('OK')"` runs from backend/ without import errors
  </verify>
  <done>All 15+ routers import get_current_user from app.core.dependencies. No router references auth_enhanced or security modules. Custom role CRUD endpoints removed from rbac router.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy auth files + clean up main.py</name>
  <files>
    librework/backend/app/core/auth_enhanced.py
    librework/backend/app/core/security.py
    librework/backend/app/core/auth_simple.py
    librework/backend/app/api/v1/auth.py
    librework/backend/app/api/v1/auth_enhanced.py
    librework/backend/app/main.py
  </files>
  <action>
    **Step 1: Delete legacy core auth files:**
    - DELETE `backend/app/core/auth_enhanced.py` — the entire custom JWT auth system (authenticate_user, create_tokens, password hashing, get_current_user dict version)
    - DELETE `backend/app/core/security.py` — JWT encode/decode with python-jose, HTTPBearer scheme
    - DELETE `backend/app/core/auth_simple.py` (if it exists) — redundant auth module

    **Step 2: Delete legacy auth routers:**
    - DELETE `backend/app/api/v1/auth.py` — Supabase Auth endpoints (register, login via Supabase)
    - DELETE `backend/app/api/v1/auth_enhanced.py` — Custom JWT endpoints (register, login, refresh, password reset, email verification)

    **Step 3: Update `backend/app/main.py`:**
    - REMOVE the import of `auth_router` (from api/v1/auth.py)
    - REMOVE the import of `auth_enhanced_router` (from api/v1/auth_enhanced.py)
    - REMOVE the `app.include_router(auth_router, ...)` call
    - REMOVE the `app.include_router(auth_enhanced_router, ...)` call
    - Keep all other router registrations unchanged
    - REMOVE any imports of `auth_enhanced`, `security`, or `auth_simple` from core if main.py imports them directly

    **Step 4: Verify backend starts:**
    - Run `cd backend && python -c "from app.main import app; print('FastAPI app created OK')"` to check that the app initializes without import errors
    - If there are remaining import errors, trace and fix them (likely a missed router that still imports a deleted module)
  </action>
  <verify>
    1. `ls librework/backend/app/core/auth_enhanced.py 2>/dev/null` — file does not exist
    2. `ls librework/backend/app/core/security.py 2>/dev/null` — file does not exist
    3. `ls librework/backend/app/api/v1/auth.py 2>/dev/null` — file does not exist
    4. `ls librework/backend/app/api/v1/auth_enhanced.py 2>/dev/null` — file does not exist
    5. `python -c "from app.main import app; print(len(app.routes), 'routes')"` runs from backend/ without errors
    6. `grep -r "auth_enhanced\|from app.core.security" librework/backend/app/` returns NO matches
  </verify>
  <done>All legacy auth files deleted (auth_enhanced.py core + router, security.py, auth_simple.py, auth.py router). main.py no longer registers legacy auth routers. Backend app initializes without import errors. Zero references to deleted modules remain.</done>
</task>

</tasks>

<verification>
1. `grep -r "python-jose\|from jose\|from passlib\|auth_enhanced\|from app.core.security" librework/backend/app/` returns ZERO matches
2. Backend FastAPI app starts without import errors
3. All remaining routers use `from app.core.dependencies import get_current_user`
4. Legacy auth files are deleted from disk
</verification>

<success_criteria>
- Every API router uses the unified `get_current_user` from `app.core.dependencies`
- Legacy files are gone: `core/auth_enhanced.py`, `core/security.py`, `core/auth_simple.py`, `api/v1/auth.py`, `api/v1/auth_enhanced.py`
- `main.py` only registers non-auth routers (plus any new Stack Auth webhook router added in Plan 05)
- Backend starts cleanly with `python -c "from app.main import app"`
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-migration/01-03-SUMMARY.md`
</output>
