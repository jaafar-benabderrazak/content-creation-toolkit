---
phase: 11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - librework/frontend/playwright.config.ts
  - librework/frontend/e2e/demo-flow.spec.ts
  - librework/frontend/e2e/explore-map.spec.ts
  - librework/frontend/e2e/mobile-viewport.spec.ts
  - librework/frontend/e2e/booking-flow.spec.ts
autonomous: true
requirements: [SEARCH-01, UX-01]

must_haves:
  truths:
    - "Playwright config includes a mobile-chrome project using Pixel 5 device"
    - "Demo mode E2E test verifies owner and customer demo flows render correct UI"
    - "Explore map E2E test verifies map toggle shows/hides map container"
    - "Mobile viewport E2E tests verify hamburger menu, map/list exclusive toggle, and booking card reflow"
    - "Booking flow E2E test verifies form submission with mocked API"
    - "All E2E tests pass via npx playwright test"
  artifacts:
    - path: "librework/frontend/playwright.config.ts"
      provides: "Mobile-chrome project with Pixel 5 device"
      contains: "Pixel 5"
    - path: "librework/frontend/e2e/demo-flow.spec.ts"
      provides: "Demo owner and customer flow tests"
      contains: "demo"
    - path: "librework/frontend/e2e/explore-map.spec.ts"
      provides: "Map toggle and marker visibility tests"
      contains: "map"
    - path: "librework/frontend/e2e/mobile-viewport.spec.ts"
      provides: "Mobile viewport layout verification tests"
      contains: "viewport"
    - path: "librework/frontend/e2e/booking-flow.spec.ts"
      provides: "Booking form interaction tests with mocked API"
      contains: "booking"
  key_links:
    - from: "librework/frontend/playwright.config.ts"
      to: "librework/frontend/e2e/*.spec.ts"
      via: "testDir: './e2e'"
      pattern: "testDir"
---

<objective>
Add Playwright E2E tests for demo mode flow, map interaction, booking form, and mobile viewport layouts. Add a mobile-chrome project (Pixel 5) to the Playwright config so all tests run against mobile viewports too.

Purpose: Phase 10 tests are shallow API mock verifications. Phase 11 adds real UI interaction tests covering the demo flow (untested key feature), map toggle, mobile-specific layouts from Plan 02, and booking form submission. The mobile project in Playwright config catches responsive regressions automatically.

Output: 4 new spec files, updated playwright.config.ts with mobile project.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing/11-RESEARCH.md
@.planning/phases/11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing/11-01-SUMMARY.md
@.planning/phases/11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing/11-02-SUMMARY.md
@librework/frontend/playwright.config.ts
@librework/frontend/e2e/explore-geolocation.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile-chrome project to Playwright config and create demo-flow + explore-map spec files</name>
  <files>librework/frontend/playwright.config.ts, librework/frontend/e2e/demo-flow.spec.ts, librework/frontend/e2e/explore-map.spec.ts</files>
  <action>
  **Update playwright.config.ts:**

  Add a second project entry for mobile:
  ```
  {
    name: 'mobile-chrome',
    use: { ...devices['Pixel 5'] },
  }
  ```
  Keep the existing chromium project. Keep all other config unchanged (testDir, timeout, webServer, etc.).

  **Create e2e/demo-flow.spec.ts:**

  Test demo mode -- this is a key untested feature. All backend calls must be mocked via `page.route()` per Phase 10 convention.

  Tests:
  1. `'demo owner flow shows demo banner'`: Navigate to `/en`. Click the Demo button (role: button, name matches /demo/i). Click the Owner option (button matching /owner/i). Assert that the nav element contains text matching /demo/i (demo banner visible).
  2. `'demo customer flow shows demo banner'`: Same pattern but click Customer option. Assert demo banner.
  3. `'exiting demo mode removes banner'`: Enter demo mode as owner, then find and click the exit/close demo button. Assert demo banner is gone.

  Mock all `/api/v1/*` routes to return empty arrays or 200 responses to prevent network errors.

  **Create e2e/explore-map.spec.ts:**

  Test map toggle behavior on the explore page.

  Tests:
  1. `'map toggle shows and hides map container'`: Navigate to `/en`. Find and click the Map toggle button. Assert a `.leaflet-container` element becomes visible (use `page.locator('.leaflet-container')`). Click Map toggle again. Assert the leaflet container is hidden or removed.
  2. `'map renders establishment markers when shown'`: Navigate to `/en`. Mock Google Maps API responses if needed. Toggle map on. Look for custom marker elements (`.custom-marker` div icons) -- if none visible (no API key), at least verify the map container rendered without error.

  Mock all `/api/v1/*` routes. For Google Maps, mock `maps.googleapis.com` requests to return empty responses to prevent errors in CI where no API key exists.
  </action>
  <verify>Run `cd librework/frontend && npx playwright test demo-flow.spec.ts explore-map.spec.ts --project=chromium` -- tests pass. Run with `--project=mobile-chrome` to verify mobile project works.</verify>
  <done>Playwright config has mobile-chrome project. Demo flow tests verify owner/customer demo entry and exit. Explore map tests verify toggle behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile-viewport and booking-flow spec files</name>
  <files>librework/frontend/e2e/mobile-viewport.spec.ts, librework/frontend/e2e/booking-flow.spec.ts</files>
  <action>
  **Create e2e/mobile-viewport.spec.ts:**

  Use `test.use({ viewport: { width: 375, height: 667 } })` to force mobile viewport for all tests in this file regardless of project.

  Tests:
  1. `'navbar shows hamburger menu on mobile'`: Navigate to `/en`. Assert a hamburger/menu button is visible (look for a button with an SVG inside that is the mobile menu trigger -- the Sheet trigger in Navbar). Assert that desktop nav links are NOT visible (they should be hidden via responsive classes).
  2. `'explore page map/list toggle is exclusive on mobile'`: Navigate to `/en`. The results grid should be visible initially. Click the Map toggle button. Assert the results grid is NOT visible (the `showList` condition hides it). Assert the leaflet container IS visible. Click Map toggle again. Assert results grid is visible again.
  3. `'establishment details booking card is not sticky on mobile'`: Navigate to `/en`. Navigate to a details page (click any establishment card or go directly to a details URL if available). Scroll down. The booking card should be visible but NOT stuck to the top (verify by checking it does NOT have `sticky` in its computed classes or simply verify it's present in the DOM flow below the content).

  Mock all API routes.

  **Create e2e/booking-flow.spec.ts:**

  Test the booking form interaction with mocked backend.

  Tests:
  1. `'booking form submits reservation'`: Navigate to `/en`. Click an establishment card from the explore page. On the details page, select a space from the dropdown, pick a date, pick a time, set duration. Click the Book/Reserve button. Assert the confirmation dialog/sheet appears with booking details. Mock the `/api/v1/reservations` POST to return 201 with a reservation object.
  2. `'booking form validates required fields'`: Navigate to details page. Try to click Book without filling required fields. Assert the button is disabled or an error message appears (depending on current validation behavior).

  Mock all API routes. Use `page.route('**/api/v1/**', ...)` to intercept all backend calls.
  </action>
  <verify>Run `cd librework/frontend && npx playwright test mobile-viewport.spec.ts booking-flow.spec.ts --project=chromium` -- tests pass. Run `npx playwright test --project=mobile-chrome` to verify all tests pass in mobile viewport too.</verify>
  <done>Mobile viewport tests verify hamburger menu, exclusive map/list toggle, and booking card reflow. Booking flow tests verify form submission with mocked API. All tests pass on both desktop and mobile-chrome projects.</done>
</task>

</tasks>

<verification>
1. `cd librework/frontend && npx playwright test` -- all tests pass (both chromium and mobile-chrome projects)
2. `grep -n "Pixel 5" playwright.config.ts` confirms mobile project
3. `ls e2e/demo-flow.spec.ts e2e/explore-map.spec.ts e2e/mobile-viewport.spec.ts e2e/booking-flow.spec.ts` -- all 4 files exist
4. Total test count increased from Phase 10's 19 tests by at least 8 new tests
</verification>

<success_criteria>
- playwright.config.ts has mobile-chrome project with Pixel 5 device
- 4 new E2E spec files created covering demo flow, map interaction, mobile layouts, and booking form
- All new tests pass on both chromium and mobile-chrome projects
- All existing Phase 10 E2E tests continue to pass
- Tests use page.route() mocking for all backend API calls (no live backend required)
</success_criteria>

<output>
After completion, create `.planning/phases/11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing/11-03-SUMMARY.md`
</output>
