---
phase: 11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - librework/frontend/src/components/ui/responsive-dialog.tsx
  - librework/frontend/src/components/ExplorePage.tsx
  - librework/frontend/src/components/EstablishmentDetails.tsx
  - librework/frontend/src/components/UserDashboard.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "On mobile (375px), ExplorePage shows EITHER the map OR the list -- not both stacked"
    - "On mobile, the booking card in EstablishmentDetails scrolls naturally below content (no sticky overlap)"
    - "On mobile, the confirmation dialog renders as a bottom sheet drawer instead of a centered modal"
    - "On mobile, UserDashboard tabs are horizontally scrollable without clipping"
    - "On desktop (1280px), all existing layouts remain unchanged"
  artifacts:
    - path: "librework/frontend/src/components/ui/responsive-dialog.tsx"
      provides: "ResponsiveDialog component -- Dialog on desktop, Sheet on mobile"
      exports: ["ResponsiveDialog"]
    - path: "librework/frontend/src/components/ExplorePage.tsx"
      provides: "Mobile-exclusive map/list toggle using useIsMobile"
      contains: "useIsMobile"
    - path: "librework/frontend/src/components/EstablishmentDetails.tsx"
      provides: "Booking card with lg-only sticky, ResponsiveDialog for confirmation"
      contains: "lg:sticky"
    - path: "librework/frontend/src/components/UserDashboard.tsx"
      provides: "Scrollable tabs on mobile"
      contains: "overflow-x-auto"
  key_links:
    - from: "librework/frontend/src/components/EstablishmentDetails.tsx"
      to: "librework/frontend/src/components/ui/responsive-dialog.tsx"
      via: "import ResponsiveDialog"
      pattern: "ResponsiveDialog"
    - from: "librework/frontend/src/components/ExplorePage.tsx"
      to: "librework/frontend/src/components/ui/use-mobile.ts"
      via: "import useIsMobile"
      pattern: "useIsMobile"
---

<objective>
Mobile responsive audit and fix across ExplorePage, EstablishmentDetails, and UserDashboard. Create a shared ResponsiveDialog component that renders Dialog on desktop and Sheet (bottom drawer) on mobile.

Purpose: On 375px viewports, the current layout has multiple issues: both map and list stack vertically on ExplorePage (too tall), the booking card overlaps content via sticky positioning, the confirmation dialog is a narrow centered box, and dashboard tabs clip. This plan fixes all of them.

Output: ResponsiveDialog component, mobile-exclusive map/list toggle, responsive booking card, scrollable dashboard tabs.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing/11-RESEARCH.md
@librework/frontend/src/components/ui/use-mobile.ts
@librework/frontend/src/components/ExplorePage.tsx
@librework/frontend/src/components/EstablishmentDetails.tsx
@librework/frontend/src/components/UserDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResponsiveDialog component and fix EstablishmentDetails mobile layout</name>
  <files>librework/frontend/src/components/ui/responsive-dialog.tsx, librework/frontend/src/components/EstablishmentDetails.tsx</files>
  <action>
  **Create `responsive-dialog.tsx`:**

  New file at `librework/frontend/src/components/ui/responsive-dialog.tsx`. Mark as `'use client'`.

  Import `useIsMobile` from `@/components/ui/use-mobile`. Import `Dialog`, `DialogContent` from `@/components/ui/dialog`. Import `Sheet`, `SheetContent` from `@/components/ui/sheet`.

  Export `ResponsiveDialog` component with props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `children: React.ReactNode`. On mobile (`useIsMobile()` returns true), render Sheet with `side="bottom"` and classes `rounded-t-xl max-h-[90vh] overflow-y-auto`. On desktop, render standard Dialog with DialogContent wrapping children.

  **Fix EstablishmentDetails.tsx:**

  1. Replace `import { Dialog, DialogContent, DialogHeader, DialogTitle } from './ui/dialog'` with `import { ResponsiveDialog } from './ui/responsive-dialog'` plus keep `import { DialogHeader, DialogTitle } from './ui/dialog'` (these sub-components work inside both Dialog and Sheet).

  2. Change `<Dialog open={showConfirmation} onOpenChange={setShowConfirmation}>` to `<ResponsiveDialog open={showConfirmation} onOpenChange={setShowConfirmation}>`. Change closing `</Dialog>` to `</ResponsiveDialog>`. Remove the wrapping `<DialogContent>` since ResponsiveDialog already provides the content wrapper. Keep `<DialogHeader>` and `<DialogTitle>` inside.

  3. Fix booking card sticky: Change `className="sticky top-20 border-gray-200"` on the Card at line ~177 to `className="lg:sticky lg:top-20 border-gray-200"`. This prevents sticky overlap on mobile while preserving desktop behavior.
  </action>
  <verify>Run `cd librework/frontend && npx tsc --noEmit` -- no type errors. Verify `responsive-dialog.tsx` exists. Grep for `lg:sticky` in EstablishmentDetails.tsx.</verify>
  <done>ResponsiveDialog renders as bottom sheet on mobile and centered modal on desktop. Booking card no longer has sticky overlap on mobile. Confirmation dialog is a drawer on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile map/list toggle in ExplorePage and scrollable tabs in UserDashboard</name>
  <files>librework/frontend/src/components/ExplorePage.tsx, librework/frontend/src/components/UserDashboard.tsx</files>
  <action>
  **In ExplorePage.tsx:**

  1. Add import: `import { useIsMobile } from './ui/use-mobile'`.

  2. Inside the component, call the hook: `const isMobile = useIsMobile()`.

  3. Compute `showList`: `const showList = !isMobile || !showMap`. This means: on desktop, list always shows (map shows above it when toggled). On mobile, map and list are exclusive -- toggling map hides list.

  4. Wrap the results grid (`<div className="grid gap-4 md:grid-cols-2">...</div>`) and the empty state and the results count in a condition: only render when `showList` is true. Specifically: wrap the results count div, the loading spinner, the results grid, and the empty state -- everything after the map section and before the closing `</div>` of the page container -- in `{showList && (<>...</>)}`.

  5. On mobile when map is showing, add a floating "Show List" button below the map toggle area so users can switch back. The existing Map toggle button already handles this -- clicking it again sets `showMap = false` which shows the list. No extra button needed, the toggle is sufficient.

  **In UserDashboard.tsx:**

  1. Find the `<TabsList className="bg-white">` element.

  2. Change to: `<TabsList className="bg-white overflow-x-auto w-full justify-start">`.

  3. Add `whitespace-nowrap` class to each `<TabsTrigger>`: e.g. `<TabsTrigger value="upcoming" className="whitespace-nowrap">`. Apply to all 5 TabsTrigger elements (upcoming, pending, past, cancelled, payments).
  </action>
  <verify>Run `cd librework/frontend && npx tsc --noEmit` -- no type errors. Grep for `useIsMobile` in ExplorePage.tsx. Grep for `overflow-x-auto` in UserDashboard.tsx. Grep for `showList` in ExplorePage.tsx.</verify>
  <done>On mobile, ExplorePage shows map OR list exclusively via toggle. UserDashboard tabs scroll horizontally on narrow screens without clipping. Desktop layouts unchanged.</done>
</task>

</tasks>

<verification>
1. `cd librework/frontend && npx tsc --noEmit` passes with zero errors
2. `ls src/components/ui/responsive-dialog.tsx` confirms file exists
3. `grep -n "lg:sticky" src/components/EstablishmentDetails.tsx` shows scoped sticky
4. `grep -n "ResponsiveDialog" src/components/EstablishmentDetails.tsx` shows usage
5. `grep -n "useIsMobile" src/components/ExplorePage.tsx` shows mobile detection
6. `grep -n "showList" src/components/ExplorePage.tsx` shows exclusive toggle logic
7. `grep -n "overflow-x-auto" src/components/UserDashboard.tsx` shows scrollable tabs
</verification>

<success_criteria>
- ResponsiveDialog component exists and renders Sheet on mobile, Dialog on desktop
- EstablishmentDetails booking card uses lg:sticky only
- EstablishmentDetails confirmation dialog uses ResponsiveDialog
- ExplorePage map/list are exclusive on mobile, coexist on desktop
- UserDashboard tabs scroll horizontally on mobile
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-real-map-data-mobile-responsive-polish-and-playwright-e2e-testing/11-02-SUMMARY.md`
</output>
