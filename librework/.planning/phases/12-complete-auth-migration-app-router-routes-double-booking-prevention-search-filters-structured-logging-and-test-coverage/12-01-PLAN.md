---
phase: 12-complete-auth-migration-app-router-routes-double-booking-prevention-search-filters-structured-logging-and-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/core/security.py
  - backend/app/core/auth_simple.py
  - backend/app/core/auth_enhanced.py
  - backend/app/api/v1/auth.py
  - backend/app/api/v1/auth_enhanced.py
  - backend/app/core/supabase.py
  - backend/app/core/logging.py
  - backend/requirements.txt
autonomous: true
requirements: [AUTH-05, INFRA-02]

must_haves:
  truths:
    - "No legacy auth files exist in backend (security.py, auth_simple.py, auth_enhanced.py, auth.py, auth_enhanced.py router)"
    - "No python-jose or passlib imports remain anywhere in the codebase"
    - "Backend logs are structured JSON via structlog (no print statements in production code)"
    - "FastAPI /docs shows no /auth/register or /auth/login endpoints from legacy routers"
  artifacts:
    - path: "backend/app/core/logging.py"
      provides: "structlog configuration with JSON renderer and contextvars"
      exports: ["configure_logging", "get_logger"]
    - path: "backend/app/main.py"
      provides: "Clean main with no legacy router imports, logging middleware added"
      contains: "logging_middleware"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/core/logging.py"
      via: "configure_logging() call at startup"
      pattern: "configure_logging"
    - from: "backend/app/core/supabase.py"
      to: "backend/app/core/logging.py"
      via: "structlog.get_logger() replacing print()"
      pattern: "structlog\\.get_logger"
---

<objective>
Delete all legacy auth files (security.py, auth_simple.py, auth_enhanced.py, auth.py router, auth_enhanced.py router), remove their imports from main.py, add structlog with JSON rendering and request context middleware, and replace all print() statements with structured log calls.

Purpose: Eliminates dead auth code that conflicts with Stack Auth and establishes structured logging foundation.
Output: Clean backend with no legacy auth, structured JSON logging.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-complete-auth-migration-app-router-routes-double-booking-prevention-search-filters-structured-logging-and-test-coverage/12-RESEARCH.md
@backend/app/main.py
@backend/app/core/supabase.py
@backend/app/core/security.py
@backend/app/core/auth_simple.py
@backend/app/core/auth_enhanced.py
@backend/app/api/v1/auth.py
@backend/app/api/v1/auth_enhanced.py
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy auth files and remove router registrations from main.py</name>
  <files>
    backend/app/core/security.py
    backend/app/core/auth_simple.py
    backend/app/core/auth_enhanced.py
    backend/app/api/v1/auth.py
    backend/app/api/v1/auth_enhanced.py
    backend/app/main.py
  </files>
  <action>
    DELETE the following files entirely:
    - `backend/app/core/security.py` (python-jose + passlib legacy JWT)
    - `backend/app/core/auth_simple.py` (duplicate of security.py)
    - `backend/app/core/auth_enhanced.py` (CivilDocPro-style custom JWT)
    - `backend/app/api/v1/auth.py` (Supabase Auth register/login router)
    - `backend/app/api/v1/auth_enhanced.py` (duplicate auth router)

    In `backend/app/main.py`:
    - Remove the import lines for `auth` and `auth_enhanced` routers (the lines that import from `app.api.v1.auth` and `app.api.v1.auth_enhanced`)
    - Remove the `app.include_router()` calls for those two routers
    - Keep ALL other router registrations (establishments, reservations, users, payments, email, etc.)
    - Keep the `dependencies.py` and `stack_auth.py` imports — those are the correct Stack Auth path

    Verify no other file imports from the deleted files by grepping for `from app.core.security`, `from app.core.auth_simple`, `from app.core.auth_enhanced`, `from app.api.v1.auth import`, `from app.api.v1.auth_enhanced`. If any import is found, update that file to remove the dead import.

    Do NOT touch `backend/app/core/dependencies.py` or `backend/app/core/stack_auth.py` — those are the correct auth path.
  </action>
  <verify>
    Run: `cd backend && grep -r "python-jose\|from jose\|from passlib\|auth_simple\|auth_enhanced\|from app.core.security" app/ --include="*.py"` — must return zero matches.
    Run: `cd backend && python -c "from app.main import app; print('OK')"` — must print OK without import errors.
  </verify>
  <done>All 5 legacy auth files deleted; main.py has no legacy router imports; no python-jose/passlib imports remain in backend.</done>
</task>

<task type="auto">
  <name>Task 2: Add structlog, configure logging middleware, replace print() with structured logs</name>
  <files>
    backend/app/core/logging.py
    backend/app/main.py
    backend/app/core/supabase.py
    backend/requirements.txt
  </files>
  <action>
    1. Add `structlog>=24.0` to `backend/requirements.txt`. Run `cd backend && pip install -r requirements.txt`.

    2. Create `backend/app/core/logging.py`:
       - `configure_logging()` function that calls `structlog.configure()` with processors: `merge_contextvars`, `add_log_level`, `TimeStamper(fmt="iso")`, `JSONRenderer()`.
       - `wrapper_class=structlog.make_filtering_bound_logger(logging.INFO)`
       - `logger_factory=structlog.PrintLoggerFactory()`
       - Export a module-level `logger = structlog.get_logger()` for convenience import.

    3. In `backend/app/main.py`:
       - Import and call `configure_logging()` before app creation.
       - Add an HTTP middleware that: clears contextvars, binds `method`, `path`, `request_id` (uuid4) to contextvars, calls `call_next`, logs `request` event with `status_code` after response. Use `@app.middleware("http")` decorator.

    4. In `backend/app/core/supabase.py`:
       - Replace all `print()` calls with `structlog.get_logger().info(...)` or `.warning(...)` as appropriate.
       - The 5 print statements at module level become structured log calls with meaningful event names (e.g., `supabase_config_loaded`, `supabase_client_created`).
       - Do NOT change any Supabase client logic — only replace print with structlog.

    5. Search entire backend for remaining `print(` calls: `grep -rn "print(" app/ --include="*.py"`. Replace any found in production code with structlog calls. Leave print in test files.
  </action>
  <verify>
    Run: `cd backend && python -c "from app.core.logging import configure_logging; configure_logging(); print('OK')"` — must succeed.
    Run: `cd backend && grep -rn "^[^#]*print(" app/ --include="*.py"` — must return zero matches in production code (tests excluded).
    Run: `cd backend && python -c "from app.main import app; print('OK')"` — must succeed with structured log output visible.
  </verify>
  <done>structlog configured with JSON renderer; HTTP request logging middleware active; all print() replaced with structured log calls in backend/app/.</done>
</task>

</tasks>

<verification>
- `cd backend && python -c "from app.main import app"` starts without errors
- No legacy auth files exist: `ls backend/app/core/security.py backend/app/core/auth_simple.py backend/app/core/auth_enhanced.py backend/app/api/v1/auth.py backend/app/api/v1/auth_enhanced.py 2>&1` shows "No such file"
- `grep -r "from jose\|from passlib" backend/app/` returns nothing
- `grep -rn "^[^#]*print(" backend/app/ --include="*.py"` returns nothing
</verification>

<success_criteria>
- 5 legacy auth files deleted
- main.py has zero legacy router imports
- structlog configured with JSON output
- HTTP middleware logs every request with method, path, request_id, status
- Zero print() calls remain in backend/app/
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-auth-migration-app-router-routes-double-booking-prevention-search-filters-structured-logging-and-test-coverage/12-01-SUMMARY.md`
</output>
