---
phase: 12-complete-auth-migration-app-router-routes-double-booking-prevention-search-filters-structured-logging-and-test-coverage
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/app/[locale]/explore/page.tsx
  - frontend/src/app/[locale]/establishments/[id]/page.tsx
  - frontend/src/app/[locale]/dashboard/page.tsx
  - frontend/src/app/[locale]/owner/dashboard/page.tsx
  - frontend/src/app/[locale]/owner/admin/page.tsx
  - frontend/src/app/home-client.tsx
  - frontend/src/components/Navbar.tsx
  - frontend/src/components/HomePage.tsx
  - frontend/src/components/ExplorePage.tsx
  - frontend/src/components/EstablishmentDetails.tsx
  - frontend/src/components/UserDashboard.tsx
  - frontend/src/components/OwnerDashboard.tsx
  - frontend/src/components/OwnerAdminPage.tsx
autonomous: true
requirements: [UX-03, AUTH-06]

must_haves:
  truths:
    - "Each major view has its own URL route (/explore, /establishments/[id], /dashboard, /owner/dashboard, /owner/admin)"
    - "Browser back/forward navigation works between all pages"
    - "Navbar links use router.push (via next-intl useRouter) not onNavigate callbacks"
    - "Navbar reflects auth state correctly (Stack Auth useUser)"
    - "Protected routes redirect unauthenticated users to login"
  artifacts:
    - path: "frontend/src/app/[locale]/explore/page.tsx"
      provides: "Explore page route"
      min_lines: 5
    - path: "frontend/src/app/[locale]/establishments/[id]/page.tsx"
      provides: "Establishment detail page route with dynamic ID"
      min_lines: 5
    - path: "frontend/src/app/[locale]/dashboard/page.tsx"
      provides: "User dashboard route (auth-guarded)"
      min_lines: 5
    - path: "frontend/src/app/[locale]/owner/dashboard/page.tsx"
      provides: "Owner dashboard route (auth-guarded)"
      min_lines: 5
    - path: "frontend/src/app/[locale]/owner/admin/page.tsx"
      provides: "Owner admin route (auth-guarded)"
      min_lines: 5
  key_links:
    - from: "frontend/src/components/Navbar.tsx"
      to: "frontend/src/app/[locale]/explore/page.tsx"
      via: "router.push('/explore') from next-intl/navigation useRouter"
      pattern: "router\\.push.*explore"
    - from: "frontend/src/app/[locale]/dashboard/page.tsx"
      to: "@stackframe/stack"
      via: "useUser({ or: 'redirect' }) auth guard"
      pattern: "useUser.*redirect"
    - from: "frontend/src/app/[locale]/establishments/[id]/page.tsx"
      to: "frontend/src/components/EstablishmentDetails.tsx"
      via: "params.id passed as prop"
      pattern: "params\\.id|useParams"
---

<objective>
Extract all 6 SPA views from home-client.tsx into proper App Router routes under [locale]/. Replace all onNavigate callback threading with next-intl useRouter navigation. Add auth guards on protected routes. Delete the home-client.tsx SPA state machine.

Purpose: Proper URL-based routing enables bookmarkable pages, browser history, and SEO.
Output: 6 route pages, updated Navbar and components, home-client.tsx deleted.
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-complete-auth-migration-app-router-routes-double-booking-prevention-search-filters-structured-logging-and-test-coverage/12-RESEARCH.md
@frontend/src/app/home-client.tsx
@frontend/src/app/[locale]/page.tsx
@frontend/src/app/[locale]/layout.tsx
@frontend/src/components/Navbar.tsx
@frontend/src/components/HomePage.tsx
@frontend/src/components/ExplorePage.tsx
@frontend/src/components/EstablishmentDetails.tsx
@frontend/src/components/UserDashboard.tsx
@frontend/src/components/OwnerDashboard.tsx
@frontend/src/components/OwnerAdminPage.tsx
@frontend/src/components/UserProfileComponent.tsx
@frontend/src/components/owner/EnhancedOwnerDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create App Router route pages for all 6 views</name>
  <files>
    frontend/src/app/[locale]/page.tsx
    frontend/src/app/[locale]/explore/page.tsx
    frontend/src/app/[locale]/establishments/[id]/page.tsx
    frontend/src/app/[locale]/dashboard/page.tsx
    frontend/src/app/[locale]/owner/dashboard/page.tsx
    frontend/src/app/[locale]/owner/admin/page.tsx
  </files>
  <action>
    Create 6 route page files. Each is a 'use client' component that imports and renders the corresponding component.

    1. `[locale]/page.tsx` — Update existing file to directly render `<HomePage />` instead of `<HomeClient />`. Remove the HomeClient import and Suspense wrapper if it only wraps HomeClient. Remove `onNavigate` prop — HomePage will use router internally.

    2. `[locale]/explore/page.tsx` — Create new file. `'use client'`. Import `ExplorePage`. Render `<ExplorePage />`. No auth guard needed.

    3. `[locale]/establishments/[id]/page.tsx` — Create new file. `'use client'`. Import `EstablishmentDetails`. Use `useParams()` from `next/navigation` to get `id`. Render `<EstablishmentDetails establishmentId={params.id} />`. No auth guard needed.

    4. `[locale]/dashboard/page.tsx` — Create new file. `'use client'`. Import `UserDashboard` and `useUser` from `@stackframe/stack`. Call `useUser({ or: 'redirect' })` to enforce auth. Render `<UserDashboard />`.

    5. `[locale]/owner/dashboard/page.tsx` — Create new file. `'use client'`. Import `OwnerDashboard` (or `EnhancedOwnerDashboard` — check which is the active one) and `useUser` from `@stackframe/stack`. Call `useUser({ or: 'redirect' })`. Render the owner dashboard component.

    6. `[locale]/owner/admin/page.tsx` — Create new file. `'use client'`. Import `OwnerAdminPage` and `useUser`. Call `useUser({ or: 'redirect' })`. Render `<OwnerAdminPage />`.

    For all protected routes (dashboard, owner/*): `useUser({ or: 'redirect' })` is Stack Auth's built-in redirect to login if unauthenticated. This replaces any custom auth guard logic.

    IMPORTANT: Use `useRouter` from `next-intl/navigation` (NOT `next/navigation`) for any navigation within these pages — locale prefix is automatically prepended.
  </action>
  <verify>
    Run: `ls frontend/src/app/*/explore/page.tsx frontend/src/app/*/establishments/*/page.tsx frontend/src/app/*/dashboard/page.tsx frontend/src/app/*/owner/dashboard/page.tsx frontend/src/app/*/owner/admin/page.tsx` — all 5 new files exist.
    Run: `cd frontend && npx next build 2>&1 | tail -20` — build succeeds (or at minimum, no route-level import errors).
  </verify>
  <done>All 6 views have their own App Router routes; protected routes use useUser({ or: 'redirect' }).</done>
</task>

<task type="auto">
  <name>Task 2: Remove onNavigate from all components, use router.push, delete home-client.tsx</name>
  <files>
    frontend/src/app/home-client.tsx
    frontend/src/components/Navbar.tsx
    frontend/src/components/HomePage.tsx
    frontend/src/components/ExplorePage.tsx
    frontend/src/components/EstablishmentDetails.tsx
    frontend/src/components/UserDashboard.tsx
    frontend/src/components/OwnerDashboard.tsx
    frontend/src/components/OwnerAdminPage.tsx
    frontend/src/components/UserProfileComponent.tsx
    frontend/src/components/owner/EnhancedOwnerDashboard.tsx
  </files>
  <action>
    For each component that currently accepts `onNavigate` as a prop:

    1. **Navbar.tsx**: Remove `onNavigate` prop. Import `useRouter` from `next-intl/navigation`. Replace all `onNavigate('explore')` calls with `router.push('/explore')`, `onNavigate('dashboard')` with `router.push('/dashboard')`, `onNavigate('home')` with `router.push('/')`, etc. The navbar already uses `useUser()` for auth state (AUTH-06 confirmed in research) — keep that.

    2. **HomePage.tsx**: Remove `onNavigate` prop. Import `useRouter` from `next-intl/navigation`. Replace `onNavigate('explore')` with `router.push('/explore')`.

    3. **ExplorePage.tsx**: Remove `onNavigate` prop. Import `useRouter` from `next-intl/navigation`. Replace `onNavigate('details', id)` with `router.push(`/establishments/${id}`)`.

    4. **EstablishmentDetails.tsx**: Remove `onNavigate` prop. Change component to accept `establishmentId` as a prop (string, from route params) instead of getting it from SPA state. Import `useRouter` from `next-intl/navigation`. Replace `onNavigate('explore')` (back button) with `router.push('/explore')` or `router.back()`.

    5. **UserDashboard.tsx**: Remove `onNavigate` prop. Import `useRouter` from `next-intl/navigation`. Replace any navigation calls with `router.push(...)`.

    6. **OwnerDashboard.tsx / EnhancedOwnerDashboard.tsx**: Remove `onNavigate` prop. Use `useRouter` from `next-intl/navigation`.

    7. **OwnerAdminPage.tsx**: Remove `onNavigate` prop. Use `useRouter` from `next-intl/navigation`.

    8. **UserProfileComponent.tsx**: If it uses `onNavigate`, remove and replace with router.

    After ALL components are updated:
    - DELETE `frontend/src/app/home-client.tsx` entirely — it is the SPA state machine and is no longer needed.
    - Verify no file imports `home-client` anymore.

    CRITICAL: Use `useRouter` from `next-intl/navigation` everywhere (not `next/navigation`) — this automatically handles locale prefixing per Phase 10-05 decision.
  </action>
  <verify>
    Run: `cd frontend && grep -rn "onNavigate" src/ --include="*.tsx" --include="*.ts"` — must return zero matches.
    Run: `ls frontend/src/app/home-client.tsx 2>&1` — must show "No such file".
    Run: `cd frontend && grep -rn "home-client" src/` — must return zero matches.
    Run: `cd frontend && npx next build 2>&1 | tail -20` — build succeeds.
  </verify>
  <done>All onNavigate props removed from all components; all navigation uses next-intl useRouter; home-client.tsx deleted; Next.js build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- Navigating to `/en/explore`, `/en/dashboard`, `/en/establishments/some-id`, `/en/owner/dashboard`, `/en/owner/admin` renders correct components
- Browser back/forward works between pages
- No `onNavigate` remains in codebase
- `home-client.tsx` does not exist
- Navbar shows correct auth state via Stack Auth useUser
</verification>

<success_criteria>
- 6 App Router routes created under [locale]/
- All components use router.push from next-intl/navigation
- Protected routes redirect unauthenticated users
- home-client.tsx SPA state machine deleted
- Zero onNavigate references in codebase
- Next.js build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-auth-migration-app-router-routes-double-booking-prevention-search-filters-structured-logging-and-test-coverage/12-03-SUMMARY.md`
</output>
